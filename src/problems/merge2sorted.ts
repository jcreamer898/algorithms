const sortedMerge = (first, second) => {
    const totalMergeLength = first.length + second.length - 1;
    const lastIndexFirst = first.length - 1;
    const lastIndexSecond = second.length - 1;
    const merged = [...first];

    while (lastIndexSecond >= 0) {
        if (index) {
        }
    }
};
